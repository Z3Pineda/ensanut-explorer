[pytest]
testpaths = tests
pythonpath = scripts
markers =
    reproduction: requires ZENODO/ source CSV tree (skipped on GitHub Actions)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
